# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: 2025 - 2026 BMO Soluciones, S.A.

# =============================================================================
# Coati Payroll - Environment Variables Example
# =============================================================================
#
# Copy this file to `.env` and customize the values for your deployment.
# Docker Compose will automatically load variables from `.env` file.
#
# IMPORTANT: Never commit `.env` to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# Database password (REQUIRED - change this!)
DB_PASSWORD=changeme

# MySQL root password (only if using MySQL)
# DB_ROOT_PASSWORD=rootchangeme

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Flask secret key (REQUIRED - must be random and secret in production!)
SECRET_KEY=change-this-to-a-random-secret-in-production

# Admin credentials (REQUIRED - change these!)
ADMIN_USER=admin
ADMIN_PASSWORD=changeme

# -----------------------------------------------------------------------------
# Network Configuration
# -----------------------------------------------------------------------------
# Port where the web app will be accessible on the host
HOST_PORT=5000

# -----------------------------------------------------------------------------
# Queue/Worker Configuration
# -----------------------------------------------------------------------------
# Number of worker threads per process (default: 8)
WORKER_THREADS=8

# Number of worker processes (default: 2)
WORKER_PROCESSES=2

# Minimum number of employees to trigger background processing (default: 100)
BACKGROUND_THRESHOLD=100

# -----------------------------------------------------------------------------
# Optional Configuration
# -----------------------------------------------------------------------------
# Application timezone (uncomment to use)
# TIMEZONE=America/Costa_Rica

# Application language (uncomment to use)
# LANGUAGE=es
